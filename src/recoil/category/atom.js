const categoryListState = {
  key: 'categoryList',
  default: [],
};

const categoryState = {
  key: 'category',
  default: {},
};

const categoryIdState = {
  key: 'categoryId',
  default: '',
};

export { categoryIdState, categoryListState, categoryState };
